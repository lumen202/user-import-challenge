# Database connection - copy to .env and adjust if needed.
# These defaults match docker-compose.yml out of the box.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=userimport
DB_USER=userimport
DB_PASSWORD=userimport
